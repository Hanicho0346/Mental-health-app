# In dev, the app uses the same LAN IP as Metro (Expo) for the API host; this URL mainly supplies the port (/api is stripped).
EXPO_PUBLIC_API_URL=http://192.168.1.10:4000/api
# Counselor user MongoDB _id for the Chat tab (optional until you set it).
# EXPO_PUBLIC_CHAT_PEER_ID=507f1f77bcf86cd799439011